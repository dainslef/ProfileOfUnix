// Notice: Syncthing rules can't add comment at line end, that may make the rule break down.

// For macOS filesystem.
.DS_Store

// For C/C++ develop and CMake.
*.o
*.a
*.so
CMakeFiles

// For Node.js.
node_modules
dist

// For most IDEs.
build
target
log
bin
out
obj
.idea
*.lock

// Need to sync git-releated files.
!.git
!.gitignore
!.gitconfig

// Need to sync some unix configurations.
!.bashrc
!.zshrc
!.vimrc
!.gtkrc-2.0

// Sync this Syncthing rule.
!.stignore_synced

// Ignore the other hidden files.
// File ".stignore" can't be synced.
// Use "#include .stignore_synced" to include this pattern in each device's ".stignore" file.
.*
